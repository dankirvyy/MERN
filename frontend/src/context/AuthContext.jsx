import { createContext } from 'react';

// This file now ONLY defines and exports the context.
// All other code (the provider component and unused imports) is removed.
export const AuthContext = createContext(null);